setuptools
wheel
Flask==3.0.3
flask-cors==6.0.2
Flask-Login==0.6.3
# Gunicorn không có trên máy bạn (do bạn dùng Windows), 
# nhưng bắt buộc phải có để chạy trên Server. 
# Tôi lấy ver mới nhất từ log cũ của bạn.
gunicorn==23.0.0
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.7.2
xgboost==3.1.2
joblib==1.2.0
python-dotenv==1.0.1
